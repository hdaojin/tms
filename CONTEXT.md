# TMS 领域上下文

本文档记录 TMS 标准体系重构后的稳定术语。所有标准、事件、训练、资料、评分、试题和考点功能都应使用这里的业务语言。

## 核心原则

- 技能项目是长期本体，不能因为世赛、国赛、省赛、选拔赛或某次训练考核而复制。
- 赛事系列和赛事级别属于事件来源与统计维度，不属于技能树本体。
- 标准技能树按整个技能项目做版本，覆盖该项目下所有长期能力领域。
- Linux、Windows、Network、Automation、Troubleshooting 等是长期能力领域，不等同于某届比赛的 A/B/C/D 模块。
- 事件模块只表达某次事件里的实际模块划分；它可以映射一个或多个能力领域。
- 覆盖统计只计算已通过的考点证据和已通过的技能映射。
- 选手技能表现从评分结果、考点证据、技能映射反推，不直接在技能点上录结果。
- 原始文件统一登记为资料资产，但上传入口仍从具体业务页面发起。

## 术语表

### 技能项目

技能项目是长期稳定的训练与竞赛对象，例如“网络系统管理”。同一个技能项目可以出现在不同赛事系列、不同赛事级别和不同年份事件中。

### 赛事系列

赛事系列表示事件来源，例如 WorldSkills、全国职业技能大赛、省赛、校内选拔等。赛事系列不创建新的技能项目。

### 赛事级别

赛事级别表示事件层级和统计权重，例如世赛、国赛、省赛、校赛、训练考核。赛事级别不创建新的技能项目。

### 事件

事件表示一次正式竞赛、选拔赛、训练考核、模拟赛或训练测试。事件绑定一个技能项目，可选绑定赛事系列、赛事级别和训练周期。

### 事件模块

事件模块表示某一次事件中的 A/B/C/D 等实际模块。事件模块不是长期标准模块，它需要通过映射关系关联到一个或多个长期能力领域。

### 训练周期

训练周期表示一段训练管理时间范围，属于训练管理边界。事件可以引用训练周期，但训练周期不属于标准体系或事件体系。

### 训练日志

训练日志记录某一天、某个训练周期、某个上传者围绕某个能力领域或训练主题提交的训练材料。训练日志文件通过资料资产登记。

### 能力领域

能力领域是技能项目下长期稳定的能力分区，例如 Linux、Windows、Network、Automation、Troubleshooting。能力领域用于组织标准技能树和分析覆盖情况，不受某届比赛模块字母变化影响。

### 标准技能树版本

标准技能树版本是一个技能项目在某个版本下的完整技能树。同一技能项目同一时间只有一个当前版本。

### 技能节点

技能节点属于某个标准技能树版本和能力领域，固定分为三类：

- `CATEGORY`: 技能分类。
- `TOPIC`: 能力主题。
- `SKILL`: 标准技能点。

正式考点映射只能落到当前技能树版本中启用的 `SKILL` 节点。

### 资料资产

资料资产是统一的文件登记对象，保存文件、原始文件名、SHA256、资料类型、业务日期、绑定业务对象、上传者和锁定状态。常见类型包括试题、评分表、评分标准、评分脚本、成绩表、结果包、训练日志、会议记录和学习资料。

### 试题

试题表示某个事件模块下的原始题目或结构化试题对象。原始文件由资料资产保存，结构化要求由试题要求保存。

### 试题要求

试题要求是从试题中抽取或手工维护的结构化要求。手工维护的试题要求可以生成已通过的考点证据。

### 评分方案

评分方案是某个事件模块的评分表解析结果，包含评分子项、评分点和评价分档。评分表原始文件由资料资产保存。

### 评分点

评分点是评分方案中的可计分条目。评分表导入后，评分点会自动生成已通过的考点证据，分值取评分点最高分。

### 评分结果

评分结果记录某个参评对象在某个评分点上的得分。技能表现通过评分结果对应的考点证据，再按技能映射权重折算到技能点。

### 考点证据

考点证据统一表达试题要求、评分点、评分标准条目、评分脚本检查项、CMP 回传评分项和人工补充考点。手工证据和评分表自动生成证据默认已通过。

### 考点技能映射

考点技能映射把考点证据关联到标准技能点。一个考点证据可以映射多个技能点，但同一时间只能有一个主技能点。覆盖统计只计算已通过映射。

### 覆盖统计

覆盖统计衡量标准技能树中的技能点是否已有已通过考点证据支撑。分值按 `estimated_mark * mapping.weight` 折算到技能点。

### 未映射考点

未映射考点是已通过但没有任何已通过技能映射的考点证据。待审核、草稿或已拒绝的映射不构成覆盖。

### 专业词库

专业词库是技能项目下用于组织中英文专业词条的命名集合。同一技能项目可以有多个专业词库。
_避免：单词本、全站术语库_

### 词条

词条是专业词库中已通过、可供浏览和学习的中英文对应项，可以表达单词、短语、命令或路径。
_避免：单词_

### 词条提案

词条提案是贡献者提交但尚未成为正式词条的候选内容；只有审核通过后才进入专业词库。

### 学习会话

学习会话是学习者针对一个专业词库开展的一次连续答题过程。

### 作答记录

作答记录是学习会话中一道题的题面、方向、提交答案、标准答案和判定结果。
