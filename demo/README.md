# Demo 应用

此应用是新模板体系的 UI 验收中心，仅在 `DEBUG=True` 时注册。

页面覆盖：

- `/demo/`：dashboard。
- `/demo/list/`：普通列表和分页 partial。
- `/demo/form/`：表单 partial。
- `/demo/detail/`：详情页。
- `/demo/htmx/`：HTMX 筛选/分页。
- `/demo/upload/`：文件上传组件。
- `/demo/print/`：打印布局。
- `/demo/states/`：空状态、alert、确认弹窗。
